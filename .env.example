# Default: Neon
DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/bodysync?sslmode=require

# Alternativa: Postgres local (docker compose --profile local-db up)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/bodysync

PORT=3000
NODE_ENV=development
